{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @compass/web db:deploy && pnpm --filter @compass/web build",
  "env": {
    "NEXT_PUBLIC_SITE_URL": "https://getcompass.space"
  }
}
