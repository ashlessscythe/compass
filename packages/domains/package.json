{
  "name": "@compass/domains",
  "version": "0.1.0",
  "private": true,
  "description": "Versioned domain pack manifests for Compass",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"No lint configured for @compass/domains\"",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@compass/api": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
